[project]
name = "ai-mouse-lab"
version = "1.0.0"
description = "Local mouse recorder, balanced Aim Lab and personal trace profiler."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
